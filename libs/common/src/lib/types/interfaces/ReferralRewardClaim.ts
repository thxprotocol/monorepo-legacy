export type TReferralRewardClaim = {
    referralRewardId: string;
    sub: string;
    uuid: string;
    amount: string;
    isApproved: boolean;
    createdAt: Date;
    poolId: string;
    metadata: string;
};
